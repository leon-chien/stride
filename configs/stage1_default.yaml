seed: 0
precision: bf16-mixed
data:
  window_frames: 64
  max_residues: 320
  residue_buckets: [128, 192, 256, 320]
model:
  encoder: gvp
  scalar_dim: 128
  vector_dim: 16
  layers: 3
  knn_k: 16
  radius_angstrom: 12.0
training:
  batch_size: 64
  gradient_accumulation: 1
  gradient_checkpointing: true
  learning_rate: 0.0003
heads:
  vicreg_dim: 64
  grl_warmup_fraction: 0.2
